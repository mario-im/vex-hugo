---
label: chi-siamo
hide_body: true
fields:
- name: titolo
  type: text
  config:
    required: false
  label: Titolo
- name: testo
  type: blocks
  template_types: []
  config:
    min: 
    max: 
  label: testo
- name: immagine
  type: file
  config:
    maxSize: 64
  label: immagine
